body{
    margin:0;
    margin-top:70px;
}

nav {
    background-color: black;
    color: white;
    width: 100vw; /*widoczne okno przeglądarki*/
    height: 70px;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    position: fixed;
    top:0;
}

ul{
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    gap: 2rem; /*odnosi się do podstawowej wielkości czcionki*/
}

li{
    list-style: none;
}

main{
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
}


main > .sidebar{
    background-color: pink;
    width: calc(100vw / 3);
    align-self: flex-start;
}

main > .elements{
    background-color: aqua;
    width: calc(100vw / 3 * 2);
    display: flex;
    flex-wrap: wrap;
    gap:1.5rem;
    justify-content: center;
    align-items: center;
    padding: 1.5rem;
}

.dog-container > img {
    height: 200px;
    width: 200px;
    object-fit: cover;
}

.dog-container{
    width: min-content;
    position: relative;
}

.dog-container > img .paw-icon {
    width: 30px !important;
    object-fit: contain !important;
    height: fit-content !important;
    position: absolute;
    top:0;
    right: 0;
}